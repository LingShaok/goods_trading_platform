<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingshao.secondhand.evaluate.dao.EvaluateDao">
    <select id="getEvaluateBySellerId" resultType="com.lingshao.secondhand.evaluate.entity.Evaluate">
        SELECT t1.id AS evaluateId,t2.name AS buyerName,t1.speed,t1.attitude,t1.accordance,
        t1.`describe`,t1.create_time AS crateTime,t1.order_number AS orderNumber
        FROM evaluate t1
        LEFT JOIN student t2 ON t1.buyer_id = t2.number
        WHERE seller_id = #{sellerId} AND deleteFlag = 0
    </select>

    <select id="getEvaluateByBuyerId" resultType="com.lingshao.secondhand.evaluate.entity.Evaluate">
        SELECT t1.id AS evaluateId,t2.name AS sellerName,t1.speed,t1.attitude,t1.accordance,
        t1.`describe`,t1.create_time AS crateTime,t1.order_number AS orderNumber
        FROM evaluate t1
        LEFT JOIN student t2 ON t1.seller_id = t2.number
        WHERE buyer_id = #{buyerId} AND deleteFlag = 0
    </select>

    <insert id="addEvaluate">
        INSERT INTO evaluate
        <trim prefix="(" suffix=")" suffixOverrides=",">
            order_number,
            seller_id,
            buyer_id,
            speed,
            attitude,
            accordance,
            <if test="Evaluate.describe">
                `describe`,
            </if>
            create_time,
            modified_time,
        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            #{Evaluate.orderNumber},
            #{Evaluate.sellerId},
            #{Evaluate.buyerId},
            #{Evaluate.speed},
            #{Evaluate.attitude},
            #{Evaluate.accordance},
            <if test="Evaluate.describe!=null">
                #{Evaluate.describe},
            </if>
            NOW(),
            NOW(),
        </trim>
    </insert>

    <update id="delEvaluate">
        UPDATE evaluate SET deleteFlag = 1,modified_time = NOW()
        WHERE id = #{evaluateId} AND buyer_id = #{buyerId}
    </update>

    <select id="isEvaluate" resultType="java.lang.Integer">
        SELECT
	        id
        FROM
	        evaluate
        WHERE
	        order_number = #{orderNumber}
        AND buyer_id = #{userNumber}
        AND deleteFlag = 0
    </select>
</mapper>